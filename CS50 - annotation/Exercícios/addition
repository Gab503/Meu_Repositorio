# include <cs50.h>
# include <stdio.h>

int main(void)
{
    int x = get_int("What is X ? ");

    int y = get_int("What is Y ? ");

    printf("%i \n", x + y );

}

Não poderá receber mais do que 4 bilhões, 2 bilhões para os números positivos, e 2 bilhões para os negativos. 
